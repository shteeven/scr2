import { ListenForScrollDirective } from './listen-for-scroll.directive';

describe('ListenForScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new ListenForScrollDirective();
    expect(directive).toBeTruthy();
  });
});
