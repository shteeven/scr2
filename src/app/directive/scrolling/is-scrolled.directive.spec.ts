import { IsScrolledDirective } from './is-scrolled.directive';

describe('IsScrolledDirective', () => {
  it('should create an instance', () => {
    const directive = new IsScrolledDirective();
    expect(directive).toBeTruthy();
  });
});
